{%- comment -%}
  Mobile quick links (scrollable, sticky under header)
  Requires a Navigation menu with handle: mobile-quick-links
{%- endcomment -%}

{%- assign mq_menu = linklists['mobile-quick-links'] | default: linklists.mobile-quick-links -%}
{%- if mq_menu and mq_menu.links.size > 0 -%}
<nav class="mobile-quicklinks" role="navigation" aria-label="Quick links">
  <div class="mq-inner">
    <ul class="mq-list" role="list">
      {%- for link in mq_menu.links -%}
        {%- assign is_active = link.active | default: false -%}
        <li class="mq-item">
          <a
            class="mq-link{% if is_active %} is-active{% endif %}"
            href="{{ link.url }}"
            {% if is_active %}aria-current="page"{% endif %}
          >
            {{ link.title }}
          </a>
        </li>
      {%- endfor -%}
    </ul>

    <!-- edge fades -->
    <div class="mq-fade mq-fade--left" aria-hidden="true"></div>
    <div class="mq-fade mq-fade--right" aria-hidden="true"></div>
  </div>
</nav>
{%- endif -%}

<style>

/* ========= GH Mobile Nav ========= */
@media (max-width: 749px) {
  :root {
    /* If your header is fixed, you can use this to offset content if needed */
    --mq-header-offset: 56px;
  }

  /* Tokenized background: configurable via CSS var; defaults to GH green */
  .gh-mobile-nav,
  .gh-mobile-nav.mobile-quicklinks--inline {
    position: static;
    top: auto;
    background: var(--gh-mobile-nav-bg, var(--gh-color-green, #21472D));
    border: 0;
    z-index: auto;
  }

  .gh-mobile-nav .mq-inner {
    position: relative;
    padding: 6px 12px;
  }

  /* Horizontal, scrollable quick-links row */
  .gh-mobile-nav .mq-list {
    display: flex;
    gap: 20px;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch; /* iOS momentum */
    scroll-snap-type: x proximity;     /* nicer snap while scrolling */
    scrollbar-width: none;             /* Firefox hide scrollbar */
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .gh-mobile-nav .mq-list::-webkit-scrollbar { display: none; } /* WebKit hide scrollbar */

  .gh-mobile-nav .mq-item {
    flex: 0 0 auto;
    scroll-snap-align: start;
  }

  /* Links */
  .gh-mobile-nav .mq-link {
    display: inline-block;
    padding: 0;
    border: 0;
    background: transparent;
    font-size: 12px;
    font-weight: 500;
    color: #fff;
    text-decoration: none;
    white-space: nowrap;
    line-height: 1.4;
    transition: opacity .18s ease;
  }

  .gh-mobile-nav .mq-link:hover {
    opacity: 0.85;
  }

  /* Active state underline */
  .gh-mobile-nav .mq-link.is-active {
    font-weight: 600;
    border-bottom: 2px solid #fff;
    padding-bottom: 2px;
  }

  /* Accessible keyboard focus */
  .gh-mobile-nav .mq-link:focus-visible {
    outline: 2px solid var(--gh-color-gold, #CCA72D);
    outline-offset: 2px;
    border-radius: 4px;
  }

  /* If you later add gradient fades at edges, keep hidden by default */
  .gh-mobile-nav .mq-fade { display: none; }
}

/* Hide on desktop/tablet */
@media (min-width: 750px) {
  .gh-mobile-nav { display: none; }
}


</style>